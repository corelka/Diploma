@*
    For more information on enabling MVC for empty projects, visit http://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@model Diploma.Models.Table

<div class="wraper">
<ul class="table" id="t-@Model.Id">
    <li>
        <p>@Model.Name</p>
        <div>
            @using (Html.BeginForm("DeleteTable", "Dashboard", new { id = Model.Id, Did = Model.DashboardId }, FormMethod.Post,new { id = Model.Id}))
            {
                <button type="submit" form="@Model.Id" class="btn btn-primary" ><i class="material-icons">delete</i></button>
            }
        </div>
    </li>
    <li>

        <button class="AddIssue btn btn-primary"><i class="material-icons">add</i></button>
        <!--<div>
            //using (Html.BeginForm("CreateIssue", "Dashboard", new { id = Model.Id }, FormMethod.Get))
            {
                <input type="submit" value="Create New Issue" />
            }                
        </div>-->        
    </li>

        @foreach (var issue in Model.Issues)
        {
            @Html.Partial("IssueSection", issue)
        }
</ul>
    </div>